[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: unit tests (no network, no disk beyond tmp)
    integration: integration tests (may touch /ai, requires ANTHROPIC_API_KEY)
asyncio_mode = auto
addopts = -ra --strict-markers
